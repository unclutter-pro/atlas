New event for trigger "{{trigger_name}}" from {{sender}}:

{{payload}}

Continue using your established workflow: investigate, decide, and handle directly. For work that exceeds a single turn, open tracked tasks and goals via the `task` CLI.
